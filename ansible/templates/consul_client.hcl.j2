datacenter = "dc1"

# Setup data dir
data_dir = "/var/opt/consul"
#encrypt = "FIXME"

{% for i in range (server_count) %}
retry_join = [
  "192.168.50.6{{ i }}",
]
{% endfor %}
bind_addr = "{{ server_addr }}"
