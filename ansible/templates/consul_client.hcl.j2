datacenter = "dc1"

# Setup data dir
data_dir = "/var/opt/consul"
#encrypt = "FIXME"

{% for i in range (1, server_count+1) %}
retry_join = [
  "192.168.50.6{{ i }}",
]
{% endfor %}
bind_addr = "{{ server_addr }}"
